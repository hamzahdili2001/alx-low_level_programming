#include <stdio.h>

/**
 * main - starts the program
 * Description: printf prints the string inside of it with new line
 * Return: it's return 0
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
